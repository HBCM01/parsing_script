# A small parsing script to open and parse webserver logs. 

# To run script:

Naviagte to the file directory. 

> cd ~/path/to/parsing_script/ruby_app/lib

> $ ruby webserver_log_parser.rb webserver.log

#  To run tests:

> rake
